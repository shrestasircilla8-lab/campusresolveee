import { useState } from "react";
import { CheckCircle2, Eye, EyeOff, GraduationCap, Loader2, ShieldCheck } from "lucide-react";
import { lovable } from "@/integrations/lovable";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { toast } from "sonner";
const demos=[['Student','student@campus.demo'],['Staff','staff@campus.demo'],['Admin','admin@campus.demo']] as const;
export function AuthScreen(){const[signup,setSignup]=useState(false),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState('');async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const result=signup?await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.origin,data:{display_name:name}}}):await supabase.auth.signInWithPassword({email,password});setBusy(false);if(result.error)return toast.error(result.error.message);toast.success(signup&&!result.data.session?'Check your email to confirm your account.':'Welcome to Campus Resolve');if(result.data.session)window.location.href='/dashboard'}async function google(){const result=await lovable.auth.signInWithOAuth('google',{redirect_uri:window.location.origin});if(result.error)toast.error(result.error.message);else if(!result.redirected)window.location.href='/dashboard'}return <div className="auth-layout"><section className="auth-story"><div className="auth-brand"><span className="brand-mark brand-mark-large">CR</span><span>Campus Resolve</span></div><div><p className="eyebrow">One campus. One clear process.</p><h1>Every issue deserves a visible resolution.</h1><p>Report campus concerns, follow every handoff, and close the loop with verified outcomes.</p></div><div className="auth-points"><span><CheckCircle2/>Live status updates</span><span><ShieldCheck/>Role-based access</span><span><GraduationCap/>Built for campus communities</span></div></section><section className="auth-panel"><div className="auth-card"><div><p className="eyebrow">Secure campus portal</p><h2>{signup?'Create student account':'Welcome back'}</h2><p className="muted-copy">{signup?'Register to report and track campus issues.':'Sign in to continue to your role dashboard.'}</p></div><form onSubmit={submit} className="form-stack">{signup?<div><Label htmlFor="name">Full name</Label><Input id="name" required minLength={2} maxLength={100} value={name} onChange={e=>setName(e.target.value)}/></div>:null}<div><Label htmlFor="email">Email</Label><Input id="email" type="email" required maxLength={255} value={email} onChange={e=>setEmail(e.target.value)}/></div><div><Label htmlFor="password">Password</Label><div className="password-wrap"><Input id="password" type={show?'text':'password'} required minLength={8} maxLength={72} value={password} onChange={e=>setPassword(e.target.value)}/><Button type="button" variant="ghost" size="icon" aria-label={show?'Hide password':'Show password'} onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</Button></div></div><Button size="lg" disabled={busy}>{busy?<Loader2 className="animate-spin"/>:null}{signup?'Create account':'Sign in'}</Button></form><div className="auth-divider"><span>or</span></div><Button variant="outline" size="lg" onClick={google}>Continue with Google</Button><Button variant="link" onClick={()=>setSignup(!signup)}>{signup?'Already registered? Sign in':'New student? Create an account'}</Button>{!signup?<div className="demo-box"><strong>Demo access</strong><p>Password for all accounts: <code>CampusDemo!2026</code></p><div>{demos.map(([role,address])=><Button key={role} type="button" variant="outline" size="sm" onClick={()=>{setEmail(address);setPassword('CampusDemo!2026')}}>{role}</Button>)}</div></div>:null}</div></section></div>}
